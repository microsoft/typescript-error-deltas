{
    "extends": "../tsconfig.settings",
    "compilerOptions": {
        "outDir": "dist", // Output files are ignored, but them in a folder for tidiness
        "emitDeclarationOnly": true,
        "isolatedModules": true,
        "types": ["node"]
    },
    "references": [
        { "path": "../src" },
    ]
}
